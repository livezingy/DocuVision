---
description: 长对话 Context 过高时建议开新对话，并给出可粘贴的约束
alwaysApply: true
---
# 长对话交接（Context handoff）

## 何时建议开新对话

用户**再次给出新任务/新命令**（不是「继续 / 提交 / 就按这个改」这类收尾）时，若满足任一条件，**先建议开新对话**，再问是否仍要在本对话继续：

- 界面、系统提示或用户口述 **Context Usage ≥ 90%**
- 虽无百分比，但已出现混乱迹象：重复已做过的分析、前后结论打架、记不清已改哪些文件、把已撤回的方案又当现行方案

**不要**等对话「自己换 context」。本对话不会自动清空。

小收尾（修一个断言、按刚才的计划 commit）不必打断。用户明确说「继续本对话」则执行，不再劝退。

## 新对话里贴什么

建议必须**自包含**，让新会话不依赖本聊天记录。用中文写一段可复制块，至少包括：

1. **目标**：这一轮要完成什么（一句话）
2. **范围**：做 / 明确不做
3. **分支与 HEAD**：当前分支名；未提交改动是否带上
4. **关键约束**：红线、验证边界（本机 vs Cloud）、官方依据优先等本任务仍有效的规则
5. **验收**：怎样算完
6. **模式**：Agent / Plan；是否允许 commit/push（默认不允许，除非用户已授权）

不要把整段 transcript 贴进建议。不要假装新对话已开始后仍在本对话里大改。

## 示例

```
建议开新对话（Agent）。Context 已偏高，再在本线程做批次 A 容易把「CSS 显示」和「crop pad」搅在一起。

请复制：

分支：fix/example（先 git pull）
只做：Figures Tab 自适应显示；撤回 crop 8% pad；CSV 分隔行带 confidence。
不做：ZIP 打包、Lite 导出。
本机无 Paddle；纯逻辑单测能跑则跑。不要 commit/push，除非我明确说。
代码英文；回复中文。
```
