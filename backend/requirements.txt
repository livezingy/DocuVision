# DocuVision backend — 唯一维护的 Python 依赖清单（合并原 requirements-colab / requirements-paddleocr-aistudio）
#
# Python: 3.10+（Paddle 3.3 官方支持 3.8–3.11；托管镜像请以镜像文档为准）
#
# 以下深度学习栈通常需在 GPU 环境中按官方指引单独安装，避免与镜像预装 CUDA 冲突：
#   paddlepaddle-gpu==3.3.x
#   paddleocr==3.3.x
#   paddlex==3.3.x
# 示例：pip install --no-deps paddlepaddle-gpu==3.3.0 paddleocr==3.3.2 paddlex==3.3.12
# Qwen2.5-VL KIE 需要 torch + transformers。GPU 环境请按 https://pytorch.org/get-started/locally/ 选择与本机 CUDA 匹配的 wheel
#（示例 CUDA 12.4：pip install torch --index-url https://download.pytorch.org/whl/cu124），避免与镜像预装 CUDA 冲突。
# 与 Paddle 同机时注意显存峰值叠加。
# 默认模型路径见 app/core/config.py KIE_QWEN_MODEL_ID（云端 ModelScope 缓存）；可通过 DOCUVISION_KIE_QWEN_MODEL_ID 覆盖。
# 可选：DOCUVISION_KIE_WARMUP=1 在进程启动后后台预加载 KIE 模型（见 docs/architecture/kie.md）。

# ============================================
# Web Framework & API
# ============================================
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
aiofiles==23.2.1
websockets==12.0
# Starlette TestClient (FastAPI 0.109 / Starlette 0.35) passes app= into httpx.Client; httpx 0.28+ removed that kwarg.
httpx>=0.24.0,<0.28.0

# ============================================
# PDF Processing
# ============================================
PyMuPDF==1.23.8
pdf2image==1.17.0
pypdf==3.17.4

# ============================================
# Image / Array
# ============================================
Pillow==10.2.0
# 与 PaddleX / 多数 Colab 约束对齐；若镜像明确使用 NumPy 2.x + Paddle 3.3，可按环境放宽上界
numpy>=1.24.0,<2.0

# ============================================
# PaddleX / hub 兼容（CRITICAL）
# ============================================
colorlog==6.8.0
ruamel.yaml==0.18.6
ujson==5.10.0
aistudio-sdk==0.2.6
modelscope>=1.28.0
langchain==0.1.0
PyYAML==6.0.2

# ============================================
# HTML Parsing (PP-Structure table output)
# ============================================
beautifulsoup4==4.12.3
lxml==5.1.0

# ============================================
# Barcode / QR（需系统 libzbar；不需要可注释 pyzbar）
# ============================================
pyzbar==0.1.9
opencv-python>=4.8.0

# ============================================
# KIE — Qwen2.5-VL（transformers + torch）
# ============================================
# torch 未锁 CUDA 变体：CPU/默认可直装；GPU 请按文件头部说明改用 PyTorch 官方 cu 索引。
torch>=2.2.0,<2.7.0
transformers>=4.45.0

# ============================================
# Data Processing & Export
# ============================================
pandas==2.1.4
openpyxl==3.1.2

# ============================================
# Document Generation
# ============================================
python-docx==1.1.0
markdown==3.5.1

# ============================================
# Utilities
# ============================================
python-dotenv==1.0.0
pydantic==2.5.3
pydantic-settings==2.1.0
loguru==0.7.2
matplotlib==3.5.3
matplotlib-inline==0.1.6

# ============================================
# Development & Testing
# ============================================
pytest>=7.4.0
pytest-asyncio>=0.23.0
