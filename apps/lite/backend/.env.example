# Optional Supabase credentials for trial PoC (Assessment / Phase 1a)
# Without these, validation records save to data/demo_validation/*.json

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Pro GPU backend (separate from Lite)
# DOCUVISION_KIE_WARMUP=1
