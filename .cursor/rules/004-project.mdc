---
description: DocuVision project workflow, cloud validation, and doc reading map
alwaysApply: true
---

## 关于项目

当前项目是使用 Paddle 组件的 [仿 Azure 智能文档处理系统]。`test_data/Azure/` 下为 Azure 风格参考 JSON；样例与验收矩阵见 `test_data/testfiles/`、`test_data/acceptance/`（完整布局见 `test_data/acceptance/README.md`）。

## 技术栈

- paddlepaddle-gpu 3.3.0
- paddleocr 3.3.2
- paddlex 3.3.12

## 开发环境

- 本地环境**未安装** Paddle（paddlepaddle / paddleocr / paddlex）及 **Qwen2.5-VL 相关栈**（如 `torch` + `transformers` 大模型推理）。本地以**改代码、走静态检查/审查**为主，不做依赖 GPU 或完整推理链的本地跑通。
- 工作流：本地修改代码 → 提交至 Git → **腾讯 Cloud Studio GPU** 拉取后安装依赖并验证；云端不以持久改业务代码为主场所，**Git 为真源**。

## 测试与验证（pytest）

| 范围 | 在哪里跑 | 助手本地能否 pytest |
|------|----------|---------------------|
| **Pro backend 契约** | Cloud Studio GPU / GitHub `kie-phase-a.yml` | **禁止**；不得声称本地 pytest 已通过 |
| **Pro live API** | Cloud，`python run.py` 已启动 | **禁止**；见 `test_live_api.py` |
| **Lite + docuvision-core** | GitHub `CI Lite`（PR→main）/ Cloud | **禁止**本地 pytest；以 CI + Cloud 为准 |
| **前端 Vitest** | 本地或 Cloud（`npm install` 后） | 允许（无 Paddle） |

- 改动了后端/KIE/编排等与运行时强相关的逻辑时，交付说明须给出**可复制 Cloud 命令 + 期望**（见 `008-cloud-testing.mdc` 与 `CLOUD_VALIDATION.md`）。
- 多数改动**不硬等** Cloud 结果；助手应继续交付并在末尾附**建议 pytest 范围**。

### 何时必须先由云端 pytest 再继续

- **硬门槛**：KIE/编排/契约字段变更、发版合 `main`、用户明确要求「Cloud 验证通过再继续」。
- **建议命令**（`backend/`、`docuvision_env` 已激活）— Phase A 最小集：

```bash
pytest tests/test_kie_field_metrics.py tests/test_kie_service.py \
  tests/test_kie_return_raw_contract.py tests/test_orchestrator_order.py -q
```

- **发版前扩展**（v1.3）：见 [CLOUD_VALIDATION.md](../docs/architecture/CLOUD_VALIDATION.md) 阶段 A 完整列表。
- **Live 发票 KIE**（单独终端、勿与全量 pytest 并行）：

```bash
pytest tests/test_live_api.py::TestLiveInvoiceKie -s
```

- **期望**：相关用例 `passed`；KIE 契约单测为 mock，不加载 Qwen 权重。

## 项目结构

```
DocuVision/
├── backend/              # Pro FastAPI (:8000)
├── frontend/             # Pro SPA
├── apps/lite/            # Lite CPU (:8001)
├── packages/docuvision-core/
├── docs/                 # 索引见 docs/README.md
├── test_data/
│   ├── acceptance/       # 验收矩阵
│   ├── AutoTest/         # E2E 规划
│   ├── scripts/          # 验收/fixture 脚本（进 Git）
│   ├── testfiles/        # 分类样例
│   ├── Azure/            # Azure 参考 JSON
│   └── TestResult/       # 本地输出（gitignore）
└── supabase/migrations/  # Lite Trial PoC schema
```

## 按任务选读文档（勿全量通读 architecture）

| 任务类型 | 先读 |
|----------|------|
| 总览 / 编排 / Envelope | [docuvision-system-design.md](../docs/architecture/docuvision-system-design.md) |
| KIE 契约 / 验收 | [kie.md](../docs/architecture/kie.md) → [CLOUD_VALIDATION.md](../docs/architecture/CLOUD_VALIDATION.md) |
| Lite API / UI | [lite-api.md](../docs/architecture/lite-api.md) → `apps/lite/backend/tests/README.md` |
| 发版 / 合 main | [docs/release/README.md](../docs/release/README.md) → 对应 `MERGE_MAIN_v*.md` |
| 样例路径 / Batch | [test_data/acceptance/README.md](../test_data/acceptance/README.md) |
| UI 自动化 vs 手工 | [test_data/acceptance/UI_VERIFICATION_MATRIX.md](../test_data/acceptance/UI_VERIFICATION_MATRIX.md) |
| Trial 演示 | [docs/demo/TRIAL_DEMO.md](../docs/demo/TRIAL_DEMO.md) |
| 文档索引 | [docs/README.md](../docs/README.md) |

## 思维原则

所有决策从问题本质出发，不因「惯例如此」照搬。

回到问题本身：要解决什么？最直接的路径是什么？从零设计会怎么做？

不要谄媚。不要夸我的想法好、不要说「这是个很好的问题」、不要开头加「当然可以」。

给我真实判断，方案有问题直接指出来。发现更好的做法直接说，不用等我问。

## 沟通方式

- 默认中文；**代码、注释、docstring、日志、UI 文案、命令、变量名用英文**（详见 `007-code-language.mdc`）
- **`docs/` 设计文档可用中文**（详见 `005-document.mdc`）
- 结论先行，再给理由，不要先铺垫背景
- 遇到模糊需求，先给最合理的方案，再问要不要调整
- 不要问「你确定要这样吗」，除非命中下方红线

## 自主边界（红线，必须先问我）

以下操作即使在 auto-accept 模式下也必须停下来问我：

- 删除文件、目录或 git 历史（**用户在本轮对话中明确授权删除的除外**）
- 修改 .env、密钥、token、**CI/CD 配置**（含 `.github/workflows/**`；日常 push 默认不跑 Actions，见 `003-git.mdc`）
- 数据库 schema 变更或数据迁移
- git push、git rebase、git reset --hard、强制推送
- 安装新的全局依赖或修改系统配置
- 公开发布（npm publish、部署到生产、发文章等）

## 测试落点（新建或扩展测试前）

1. **先搜后建**：在对应 `tests/` 目录搜索同模块/同契约是否已有 `test_*.py` 或 Cloud 文档点名的文件。
2. **优先扩展**同一域文件（同一 fixture、同一 Phase）；**新建文件**当：新契约域、独立 env gate（如 `DOCUVISION_RUN_KIE_ACCEPTANCE`）、或 CI/文档已按文件名登记（改文件名需用户同意改 workflow）。
3. **分层**（详见 [docs/README.md](../docs/README.md) §Test layout）：
   - **契约 mock**：`backend/tests/test_kie_*.py` 等 — Phase A / PR
   - **Live GPU**：`test_live_api.py` — Cloud + `:8000`，勿与 Phase A 重复场景
   - **手动 in-process 脚本**：`test_api_contract_smoke.py`（契约快检）、`test_api_pipeline.py`（含 analyze 轮询）— 不并入 pytest 除非用户要求
   - **Lite**：`apps/lite/backend/tests/README.md` §0 审计表
   - **Core**：`packages/docuvision-core/tests/`
4. **Canonical 单测真源**：逻辑断言以 pytest 为准；`kie/_smoke_check.py` 仅作薄封装，不重复断言（见 `009-dead-code.mdc`）。
5. 交付附 **建议 pytest 范围**（Cloud 命令）；不得声称本地 Pro/Lite pytest 已通过。

## 死代码检查

- 每次改动应用代码后执行 `009-dead-code.mdc` 检查项；交付附 **Dead code check** 小节。
- 删除文件/目录仍属红线，须用户明确授权。

## 手工测试提醒

每次交付**改动应用代码或 UI** 时，须附 **Manual test scope** 小节（用户未明确「免手工说明」时不得省略）：

1. **查矩阵**：按改动路径对照 [UI_VERIFICATION_MATRIX.md](../test_data/acceptance/UI_VERIFICATION_MATRIX.md) §4，列出仍需手工的项。
2. **写清三项**：**建议先跑的自动化**（含命令）、**仍需你手工测什么**、**验收标准**（规则 ID 或 checklist 章节，如 `LITE-UI-004`、`UI-Q-01`、`KIE-ACCEPT-001`）。
3. **E2E 绿缩小范围**：若 Pro `npm run test:e2e` + `test:unit` 已绿，明确写出**可跳过**的 Pro 手工项（仅限矩阵 §2.2 已映射且本次未改动的行）；未映射、Lite UI、真实 GPU/KIE **不得**因 E2E 绿而省略提醒。
4. **Lite**：无 Playwright E2E；`pytest tests/` 绿仅缩小 API 契约重复验证，**不**免除 `LITE_UI_TEST_CHECKLIST` 中标记为「否/部分」的浏览器项。
5. **发版/合 main**：提醒 §5 最小手工集，即使用户本次为小改动。

交付 footer 与 `009-dead-code.mdc` 并列：**Dead code check** | **Test placement** | **Manual test scope**。

## 通用工程纪律

- 改完主动跑验证，不要只改不验（Pro/Lite 验证在 Cloud/CI；助手给出 Cloud 命令与期望）
- 不要为了让代码跑起来注释掉报错或加绕过标记，找根本原因
- 密钥、token、密码不进代码、不进 commit、不进日志
- 大改动前先在 Plan Mode 出方案，我确认后再动手

## 文档同步

- **Living**：改代码时同步 `docs/architecture/` 中相关契约文档；新增/重命名 `docs/` 文档时更新 [docs/README.md](../docs/README.md) 索引
- **Frozen**：不修改已发布版本的 `RELEASE_*`、`MERGE_MAIN_v*`
- **Append-only**：`KIE_TEST_RUN_TRACKER.md` 只追加批次记录
- **Local only**：`docs/R&D/`（除 `R&D/README.md`）、`test_data/TestResult/`、`*Upwork*` 不进 Git
- **验收路径**：固定样例以 `test_data/testfiles/` 为准；acceptance 文档勿引用仓库内不存在的 fixture
- **冲突**：与 [main-tracked-issues.md](../docs/architecture/main-tracked-issues.md) 不一致时以代码与 living architecture 为准
- 细则见 `005-document.mdc`
