# DocuVision - PaddleOCR Only Version
# Minimal dependencies for Python 3.11
# Only includes PaddleOCR for OCR functionality

# ============================================
# AI Studio GPU (Preinstalled Stack)
# ============================================
# If you run in Baidu AI Studio GPU images WITHOUT a virtual environment,
# these are typically preinstalled and should NOT be installed/pinned here:
# - paddlepaddle-gpu==3.3.0
# - paddleocr==3.3.2
# - paddlex==3.3.12
# Installing different versions into the global environment may break the image.

# ============================================
# Web Framework & API (Minimal)
# ============================================
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
aiofiles==23.2.1
websockets==12.0



# ============================================
# PDF Processing (Minimal)
# ============================================
PyMuPDF==1.23.8
pdf2image==1.17.0
pypdf==3.17.4

# ============================================
# Image Processing (Required by PaddleOCR)
# ============================================
Pillow==10.2.0
# NumPy 2.x compatible with PaddlePaddle 3.x
# PaddlePaddle 3.0+ supports NumPy 2.x
numpy>=1.24.0

# ============================================
# HTML Parsing for table extraction (PP-Structure output)
# ============================================
beautifulsoup4==4.12.3
lxml==5.1.0

# ============================================
# Barcode Recognition (Optional - if needed)
# ============================================
# PRIMARY: PyZBar - Barcode and QR code recognition
pyzbar==0.1.9
# Note: Requires system library zbar
# Windows: Download from https://github.com/mchehab/zbar/releases
# Linux: sudo apt-get install libzbar0
# Mac: brew install zbar

# ALTERNATIVE: OpenCV - QR code detection (limited support)
# PaddleOCR 3.x supports newer OpenCV versions
opencv-python>=4.8.0

# ============================================
# Data Processing & Export
# ============================================
pandas==2.1.4
openpyxl==3.1.2

# Document Generation
python-docx==1.1.0
markdown==3.5.1

# ============================================
# Utilities
# ============================================
python-dotenv==1.0.0
pydantic==2.5.3
pydantic-settings==2.1.0
loguru==0.7.2

# ============================================
# Development & Testing (Optional)
# ============================================
# Uncomment if needed for testing
# pytest==7.4.4
# pytest-asyncio==0.23.3
# httpx==0.26.0
# requests==2.31.0

# ============================================
# KIE (PaddleNLP UIE) + aistudio-sdk (PaddleX 兼容)
# ============================================
# PaddleNLP 需 aistudio-sdk<0.3（hub.download）；PaddleX 3.3 默认用 snapshot_download。
# DocuVision 在 app.main 于 import paddlex 前注册 app.core.aistudio_compat shim。
# 安装 KIE 时请: pip install paddlenlp ... 且 aistudio-sdk==0.2.6
aistudio-sdk==0.2.6

# ============================================
# EXCLUDED DEPENDENCIES (PaddleOCR-only version)
# ============================================
# The following are NOT included in this minimal version:
# - pytesseract (Tesseract OCR fallback)
# - easyocr (EasyOCR alternative)
# - camelot-py (Table extraction fallback)
# - spacy (NLP engine)
# - hanlp (Chinese NLP engine)
# - tabula-py (Table extraction alternative)
# - layoutparser (Layout analysis fallback)
