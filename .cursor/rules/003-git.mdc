---
description: Git commit, push, branch, and GitHub Actions policy
globs:
alwaysApply: false
---
# Git 规范

## 提交规范
git 提交记录样例：[type]: [description]。一个具体的例子, docs: 更新 README 文件。
以下是 type 的枚举值：
- feat: 新增功能
- fix: 修复 bug
- docs: 文档注释
- style: 代码格式(不影响代码运行的变动)
- refactor: 重构、优化(既不增加新功能, 也不是修复bug)
- perf: 性能优化
- test: 增加测试
- chore: 构建过程或辅助工具的变动
- revert: 回退
- build: 打包

### 提交前确认分支（强制）
- **commit 前先 `git branch --show-current` 确认当前分支**，再 `git add` / `git commit`。
- 多分支并行时尤其重要：误在错误分支提交会导致 commit 落错 PR、污染 docs-only PR 或需要历史改写清理。
- 教训来源：2026-07-31 多分支并行时 test fix 误落在 docs 分支（虽未推送、本地 reset 清理，但属可避免失误）。

## 分支管理
- main/master: 主分支，保持稳定可发布状态
- develop: 开发分支，包含最新开发特性
- feature/*: 功能分支，用于开发新功能
- bugfix/*: 修复分支，用于修复bug
- release/*: 发布分支，用于准备发布

## 远端同步（GitHub）

- 在本仓库内完成**可交付的代码或前端资源变更**后，若用户明确要求「提交 / 推送到 GitHub」，或用户约定「改完即同步远端」，应在合理自检（lint、关键路径冒烟等，视改动范围）后执行：`git add` → `git commit`（遵循上文提交规范）→ `git push` 至当前分支对应的 `origin`。
- 推送前确认工作区仅包含拟交付的变更；密钥、token、`.env` 等敏感内容不得进入 commit。

## GitHub Actions 策略

- **默认 push 不跑 CI**：`.github/workflows/*.yml` 中 push 事件仅当 commit message 含 **`[run ci]`** 时才执行 job；日常 push **不要**加该标记。
- **PR 至 `main`**：路径匹配时 **自动** 运行 `CI Lite` / `KIE Phase A`（无需额外标记）。
- **手动触发**：GitHub → Actions → 选择 workflow → **Run workflow**（`workflow_dispatch`）。
- **修改 workflow**：属 CI/CD 变更（见 `004-project.mdc` 红线）。未获用户明确同意前，助手不得改 `.github/workflows/**`。
- **助手若要建议跑 Actions**：须先说明理由（例如合并前契约回归、改了 workflow 本身需验证），再询问用户；仅当用户明确同意，或用户说「激活 workflow / 跑 CI / commit 加 [run ci]」时，方可：
  - 在 commit message 中加入 `[run ci]` 再 push，或
  - 指引 / 代用户在 GitHub 上 `workflow_dispatch`。
- **日常验证**：优先 Cloud Studio 手动 pytest；Actions 用于 PR 门禁或用户点名时。

## 重要原则

- **默认**：未获用户对**提交 / 推送**的明确意图，且未触发上文「远端同步」条款时，不擅自执行 `git commit` / `git push`。
- 提交前确保代码通过所有测试（或等价验证），信息简洁、描述准确。
- 避免大型提交，尽量将变更分解为小的、相关的提交。
