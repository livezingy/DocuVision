{
  "test_files": 7,
  "cases": 67,
  "note": "Pin for the vitest unit suite (frontend/tests/unit/**/*.test.js), so it cannot shrink silently - a deleted spec file, an it.todo or a descoped case all remove protection while CI stays green (P-008's last residual; the e2e side is pinned by scripts/e2e_suite_pin.json + E1). Measured from the sources, so update this file in the same commit as any intended suite change. 2026-09-26 first pin: 7 files / 67 case declarations / 0 skips, matching `npm run test:unit` (7 passed files, 67 passed tests) on the same tree. Enforced by scripts/unit_suite_pin.py (E2)."
}
